# Pixi 项目工程架构模板（供智能体在空白项目中自举）

这是一份**可复用模板**，当你（智能体）在一个新的空pixi 项目里被要求"按这个架构搭建工程"时，把本文件内容当作操作手册直接执行，而不是重新设计一套。

本文件分两部分：**核心骨架**（任何 pixi 数据/分析项目都适用）和**科研模式扩展**（仅当项目是假设驱动的科研项目——需要反复修正科学假设、写论文——才启用，见第 5 节判断标准）。

---

## 0. 这套架构解决的问题、以及它的边界

**解决的问题**：当一个数据管线项目的编号脚本超过 10-15 个之后，人和智能体都会失去全局把控——不知道
脚本 A 依赖脚本 B 的哪个产物，不知道某个结果目录对应哪个脚本，不知道改一个脚本要不要连带改文档。这套
架构用四个机制应对：

1. **编号脚本 + 一一对应的结果目录**，让"执行顺序"和"产物归属"肉眼可查。
2. **openspec 契约先行**，新增一个分析阶段前先写清楚"这个阶段承诺产出什么、在什么条件下"，避免代码
   和意图逐渐漂移。
3. **`docs/architecture/` 三件套**（README 总览 / PIPELINE 明细表 / CONVENTIONS 规则），把"总览、
   明细、规则"三种关注点分开维护，避免互相复制正文导致改一处漏三处。

---

## 1. 目录骨架

在空白项目根目录下创建：

```
<project-root>/                # 通过pixi init <project-name>
├── pixi.toml                  # pixi 自动生成
├── pixi.lock                  # pixi 自动生成，勿手写
├── .gitignore
├── .gitattributes
├── src/                       # 可复用的共享代码（一旦启用要在 architecture 里补文档）
├── test/                      # 测试
├── scripts_py/                # 编号 Python 脚本，见第 3 节命名规则
├── scripts_r/                 # 编号 R 伴随脚本（如果有统计/绘图用使用本地的`Rscript`命令；否则不建）
├── results/                   # 每个脚本一个结果目录，NN_description/
├── openspec/                  # 通过openspec init命令自动创建的openspec文件夹
└── docs/
    ├── architecture/
    │   ├── README.md          # 总览：数据流心智模型、阅读顺序
    │   ├── PIPELINE.md         # 明细表：脚本→结果目录→契约 对照
    │   ├── CONVENTIONS.md      # 规则：命名规范、新增脚本时同步清单
    └── （数据规模说明、方法论演进文档等，见第 5 节）
```

数据目录（原始数据、大型中间产物）如果不应该进 git，用符号链接指到项目外部存储。
`datas -> /path/to/actual/datas`，并在 `.gitignore` 里排除。

---

## 2. pixi.toml 初始化

```toml
[workspace]
# pixi 自动生成

[tasks]
# 建议从项目一开始就把常用管线步骤注册成 task，而不是留空

[dependencies]
# 按需添加分析库
```

---

## 3. `scripts_py/`（+ `scripts_r/`）编号与命名规则

- **主编号**：`scripts_py/NN_description.py`，两位数序号。序号不是严格执行顺序保证，但基本按引入
  时间递增，后面的阶段大多依赖前面阶段的输出。
- **字母后缀**：`scripts_py/NN[number]_description.py`同一编号下的变体/配套脚本/额外的诊断/临时脚本用字母区分。
- **R 伴随脚本**：`scripts_r/NN_*.Rmd` 与对应 Python 脚本共享编号前缀，读取 Python 脚本写到
  `results/NN_.../` 下的计算出来的数据。不是所有 Python 脚本都需要 R 伴随（纯只读预检类脚本可以没有）。
- **结果目录**：`results/NN_description/`，目录名与脚本名（去掉字母后缀和阶段码前缀）基本一致。
  R 产物统一写入 `results/NN_.../stats/` 子目录，Python 产物留在顶层——这条约定能让人一眼分清一个
  文件是哪个语言生成的，不需要打开文件看内容。

---

## 4. `docs/architecture/` 三件套怎么写

### README.md（总览）
- 阅读顺序清单（指向 PIPELINE/CONVENTIONS/以及科研模式的 RESEARCH_LOGIC，见第 6 节）。
- 数据流心智模型：一张 ASCII 图，从原始数据到最终共享主表，再到下游各分析阶段的分支结构。
- 明确写"不在本目录维护的内容，去哪里找"——避免同一件事被写两遍然后逐渐不同步。
- "已知的孤儿/待确认项"清单：哪些脚本没有契约、哪些结果目录和脚本默认路径不一致，人工发现但还没
  修复的都记在这里，防止下次看到诡异状态时误以为是新 bug。

### PIPELINE.md（明细表）
- 一张大表：`Script | 结果目录 | 内部阶段码（可选） | R 伴随  | 用途 | 是否依赖其他脚本`。
- 用途文字直接摘自 spec 的 Purpose 或 change 的 proposal 摘要，不要另写一套说法（否则两处文字会随时间
  各自漂移）。
- 阶段合并、目录重组等结构性变更记一节"已完成的 stage 合并"，写清楚合并前后如何验证等价（这是唯一
  能让后人相信"删掉旧编号是安全的"的证据）。

### CONVENTIONS.md（规则）
- 第 3 节的命名规则原样搬过去。
- "新增/修改编号脚本时要同步的地方"清单（写 spec → 补 PIPELINE.md 一行 → 更新孤儿列表）。
- "何时该新建一个新的 architecture 文件而不是塞进现有三个文件"——只有出现全新的、跨脚本的关注点才
  新建（比如隐私边界图、共享工具函数文档、或第 6 节的 RESEARCH_LOGIC）；不要为单个脚本或单个契约单独
  建文件。

---

## 5. 科研模式扩展（仅假设驱动的科研项目启用）

**判断标准**：如果这个项目会反复修正科学假设、需要给同行评审/论文写方法论演进的说明，且"这一版为什么
改了上一版的设计"本身是需要保留的知识，才启用本节；纯数据处理/工具/工程项目跳过本节，第 1-4 节的核心
骨架已经够用。

### 5.1 版本化假设文档

`docs/研究方案_update_v*.md`（或英文项目用 `docs/research_proposal_v*.md`），每次假设/判据发生实质
修正就开一个新版本文件，不覆盖旧版本。每个版本文档建议包含：

- **版本说明与设计立场**：这一版要检验什么、和上一版比新增/修正了什么。
- **致命陷阱/既有教训**：如果这一版是在纠正上一版的判据漏洞，必须明确写清楚"上一版为什么会误判"，
  不能只写"改进了xxx"。这是让方法论演进可追溯、可被同行评审的关键信息，比结果本身更容易随时间丢失。
- **假设编号体系**（H1/H2/... 或项目自定义）+ 对应的决策门枚举。
- 已知局限、待确认的口径（留给主研究者拍板的开放问题清单）。

**重要提醒**：假设编号在跨版本时容易被复用（比如"v2 的 H4"和"v5 的 H4"指的是完全不同的假设）。当你
（智能体）在写跨版本总结文档时，一律加版本前缀消歧义（"v3.1-H4" vs "v3.4-H4"），不要假设读者会自动
分清楚。

### 5.2 RESEARCH_LOGIC.md（跨版本总览文档）

当版本化假设文档积累到 3-4 个版本以上、且各阶段决策门已经陆续产出结果后，新建
`docs/architecture/RESEARCH_LOGIC.md`，职责是回答"整条研究逻辑
是怎么走到今天的、算出了什么、意味着什么"，具体包含：

1. 核心科学问题的版本演进（每版新增/修正了什么，尤其是"发现上一版判据本身有漏洞"这类自我纠错，要
   写具体，不要写"改进了xxx"）。
2. 阶段代号 → 科学问题 → 引入版本 → 依赖关系的总表。
3. 贯穿全程不变的方法论骨架（核心判据框架、统计骨架的正交性铁律等）。
4. 当前有效版本下，从数据到最终判据的完整逻辑链图，并标注链条实际卡在哪一步、为什么（区分清楚是
   "分析方法/统计功效不足"还是"数据采集协议层面的结构性缺口"——这个区分本身对研究方案完成情况的评估
   至关重要，措辞上不要用后者的事实去支持前者式的悲观/乐观结论）。
5. 逐结果目录的完成情况表——这一节必须去实地读 results 里的结果数据，不能从方案文档推断，因为方案文档写的是"打算怎么判"，results 里的才是"实际判成了什么"。

这份文档**不复制**版本文档的论证细节，也**不复制**PIPELINE.md 的脚本清单——只在两者之间搭桥。

---

## 6. .gitignore 策略

本项目的策略（默认建议延续，尤其涉及隐私/受限数据的研究项目）：
在已有的.gitignore文件内追加（不要覆盖）

```
__pycache__/

.codex/
.claude/

docs/
openspec/
results/
datas
```

---

## 7. 智能体自举检查清单

在一个空白目录里被要求"按这个架构搭建"时，按顺序执行：

1. 确认 pixi 已安装（`pixi --version`），通过 `pixi init`初始化项目文件夹（如果已经有pixi.toml文件则说明已经手工初始化，则跳过这一步）。
2. 创建第 1 节的目录骨架（`src/`、`test/`、`scripts_py/`、`scripts_r/`、`results/`、`docs/architecture/`）。datas软连接需要人工自行创建。
3. `npm install -g @fission-ai/openspec`（如未安装），`openspec init`（如果没有openspec/目录，通过`openspec help init`查看支持的智能体，并根据智能体类型（codex或claude code等）选择合适的`openspec init --tools`命令）。
4. 使用`codegraph init`命令初始化codegraph
5. 修改 `.gitignore`（见## 6. .gitignore 策略）。
6. 创建 `docs/architecture/README.md` / `PIPELINE.md` / `CONVENTIONS.md` 三个骨架文件（可以先是空表 + 阅读顺序说明，随项目增长进行修改、填充、完善）。
7. 询问用户这是否是假设驱动的科研项目（第 6 节判断标准）；如果是，额外建好版本化假设文档的第一版
   和后续会用到的 `RESEARCH_LOGIC.md` 占位（可以先不写内容，等有 3-4 个版本后再实质填充）。
8. 每次新增分析阶段时，走 openspec 流程，并在完成后同步更新 PIPELINE.md 一行——这是长期
   保持架构不腐化的唯一机制，必要时更新architecture下的其他文件，不要因为"先写代码更快"而跳过。
9. 创建AGENTS.md文件，持久化规则，使每次新建对话都能遵循规则
---
